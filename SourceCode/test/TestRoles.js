// This script is designed to test the solidity smart contract - SuppyChain.sol -- and the various functions within
// Declare a variable and assign the compiled smart contract artifact
/*
var WoodPickerRole = artifacts.require('WoodPickerRole')

contract('WoodPickerRole', function(accounts) {

    const originWoodPickerID = accounts[1]
    it("Set Woodpicker role", async() => {
    const woodpickerrole = await WoodPickerRole.deployed()
    var iswoodpicker = false
    await woodpickerrole.addWoodPicker(originWoodPickerID);
    iswoodpicker = await woodpickerrole.isWoodPicker(originWoodPickerID);
    assert.equal(iswoodpicker, true, 'Error: woodpicker role not set')
    })
})
*/